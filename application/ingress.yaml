apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: color-app-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /blues
        pathType: Prefix
        backend:
          service:
            name: blue-service
            port:
              number: 80
      - path: /greens
        pathType: Prefix
        backend:
          service:
            name: green-service
            port:
              number: 80
      - path: /reds
        pathType: Prefix
        backend:
          service:
            name: red-service
            port:
              number: 80
